<!DOCTYPE html>
<html>
<head>
  <title>API Dokumentasi - DannTeam</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <style>
    body {
      font-family: 'Hack', monospace;
      padding: 20px;
      line-height: 1.6;
      max-width: 800px;
      margin: 0 auto;
    }

    h1, h2 {
      margin-bottom: 10px;
    }

    p {
      margin-bottom: 20px;
    }

    pre {
      background-color: #f4f4f4;
      padding: 10px;
      overflow-x: auto;
      position: relative;
    }

    .copy-btn {
      position: absolute;
      top: 0;
      right: 0;
      padding: 5px 10px;
      background-color: #3182ce;
      color: white;
      cursor: pointer;
      border-top-right-radius: 5px;
    }

    footer {
      margin-top: 40px;
      font-size: 12px;
      color: #999;
      text-align: center;
    }

    footer a {
      color: #999;
      text-decoration: none;
    }

    @media (max-width: 600px) {
      body {
        padding: 10px;
      }
    }
  </style>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body>
  <h1>API Documentation</h1>

  <h2>Upload Image</h2>
  <p>
    <strong>Endpoint:</strong> POST /remini (redirect) <br>
    <strong>Description:</strong> Mengunggah file gambar.<br>
    <strong>Parameters:</strong>
  </p>
  <h3>Contoh Node.js</h3>
    <pre class="relative"><span class="copy-btn">Copy</span><code class="language-javascript">
  const fetch = require('node-fetch');

  const uploadImage = async () => {
    const formData = new FormData();
    formData.append('image', fs.createReadStream('path/to/image.jpg'));

    const response = await fetch('https://api.dannteam.my.id/remini', {
      method: 'POST',
      body: formData,
    });

    const imageUrl = await response.text();
    console.log('Image URL:', imageUrl);
  };

  uploadImage();
    </code></pre>
  <h3>Contoh Header</h3>
  <pre class="relative"><span class="copy-btn">Copy</span><code class="language-http">
POST /remini HTTP/1.1
Host: https://api.dannteam.my.id/
Content-Type: multipart/form-data; boundary=----WebKitFormBoundary7MA4YWxkTrZu0gW
  </code></pre>
  <h3>Contoh cURL Bash</h3>
  <pre class="relative"><span class="copy-btn">Copy</span><code class="language-bash">
curl -X POST -H "Content-Type: multipart/form-data" -F "image=@/path/to/image.jpg" https://api.dannteam.my.id/remini
  </code></pre>

  <footer>
    2023 © <a href="https://instagram.com/dannalwaysalone/"><b>DannTeam.</b></a> |
    Hak Cipta Dilindungi Oleh <a href="https://dannteam.com"><b>DannTeam.</b></a>
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
  <script>
    const copyButtons = document.querySelectorAll('.copy-btn');
    copyButtons.forEach(btn => {
      btn.addEventListener('click', () => {
        const codeBlock = btn.nextElementSibling;
        const text = codeBlock.textContent;
        navigator.clipboard.writeText(text)
          .then(() => {
            Swal.fire({
              icon: 'success',
              title: 'Copied to Clipboard!',
              showConfirmButton: false,
              timer: 1500
            });
          })
          .catch(err => {
            console.error('Gagal menyalin kode: ', err);
          });
      });
    });
  </script>
</body>
</html>
